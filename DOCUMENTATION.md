# ScriptMarkDown (.smd) 記法 ＆ 動作仕様ガイド

ScriptMarkDown (`.smd`) は、VOICEVOX Engine リアルタイム音声再生に対応した劇本・シナリオ・脚本執筆用マークダウン拡張フォーマットです。
**「1台詞1行」** の原則に従い、上から下へ順番に評価・再生されます。

---

## 1. 脚本実例 ＆ 構文完全サンプル (.smd)

```smd
// ==========================================
// 1. デフォルト設定宣言 (preset ディレクティブ)
// ・上から下へ順番に評価され、後勝ちで上書きされます。
// ==========================================

// 全キャラのベース設定 (全体適用: 話速・音高・抑揚・音量・間・前後の無音時間・フォント・立ち絵)
preset.all.speed(1.1).pitch(0.02).intonation(1.0).volume(1.0).pause_length(1.2).pre_silence(0.2).post_silence(0.3).font("Noto Sans").tatie("通常")

// 特定キャラの個別の既定値 (クォート囲み可)
preset.'ずんだもん'.speed(1.25).pause_length(1.5).tatie("立ち絵A")
preset."四国めたん".speed(1.15).pitch(-0.02).post_silence(0.4).font("ポップ体")


// ==========================================
// 2. 基本セリフ ＆ 発声記法 (1台詞1行の原則)
// ==========================================

// 話者名> セリフ本文 (基本形)
ずんだもん> こんにちはなのだ！

// 話者名.スタイル名> セリフ本文 (一時的な声質変更)
四国めたん.ツンツン> ごきげんよう、勘違いしないでほしいですわ！
後鬼.人間ver.> 人間姿で挨拶しますわ！

// [表記|よみ] ルビ表記 (テキスト表示の美しさとVOICEVOX読みの補正)
// ※字幕は「本気」、VOICEVOXの発声は「マジ」と読ませます
ずんだもん> [本気|マジ]で[応援|おうえん]するのだ！

// > テロップ文章 (話者名なし、行頭が「>」のみ)
// 音声合成を行わない画面表示用テロップ行。.font()/.size()/.color()での装飾や、
// 直後に.wait(秒数)を付けての表示時間指定が可能 (省略時は約0.2秒でスキップ)
> ここぞという場面転換のテロップ！.size(1.5).color(#ffcc00).wait(2)


// ==========================================
// 3. 行末直後のパラメータ上書き (Inline Overrides)
// ==========================================

// セリフ行の末尾に直付けすることで、この1行のみ限定でパラメータを変更
ずんだもん> この1行だけ文中の間を広くして、立ち絵も切り替えるのだ！.pause_length(1.8).post_silence(0.5).tatie("ドヤ顔")


// ==========================================
// 4. 効果音 ＆ BGM 指示子 (SE & BGM Directives)
// ==========================================

// BGM再生 (ファイルパス, 音量0.0〜1.0)
.bgm('/bgm/cheerful_theme.wav', 0.3)

// SE (効果音) 再生 (単独行記述)
.se('/se/chime.wav')

// インラインSE記述 (セリフ発声と同時に効果音を並列再生)
四国めたん> チャイムが鳴りましたわ！.se('/se/chime.wav')

// 純粋な無音待機ウェイト (BGMを停止させず、指定秒数だけ音声再生をウェイト)
.wait(1.5)

// BGM再生停止 (空文字またはパスなしで停止)
.bgm('')
```

---

## 2. パラメータ一覧 (Parameters)

| メソッド名 | 説明 | デフォルト値 |
| :--- | :--- | :--- |
| **`.speed(倍率)`** | 話速倍率 (speedScale) | `1.0` |
| **`.pitch(数値)`** | 音高ピッチ (pitchScale) | `0.0` |
| **`.intonation(倍率)`** | 抑揚倍率 (intonationScale) | `1.0` |
| **`.volume(倍率)`** | 音量倍率 (volumeScale) | `1.0` |
| **`.pause_length(倍率)`** | 読点や文中の「間」の長さ倍率 (pauseLengthScale) | `1.0` |
| **`.pre_silence(秒数)`** | 発話開始前の無音時間秒数 (prePhonemeLength) | `0.1` |
| **`.post_silence(秒数)`** | 発話終了後の無音時間秒数 (postPhonemeLength) | `0.1` |
| **`.font("フォント名")`** | テキストフォント指定（将来拡張用・未実装） | - |
| **`.tatie("立ち絵名")`** | 立ち絵グラフィック指定（将来拡張用・未実装。パス検証とホバー参照のみ対応） | - |
| **`.size(数値)`** | テロップ行の文字サイズ指定（将来拡張用） | - |
| **`.color("#rrggbb")`** | テロップ行の文字色指定（将来拡張用） | - |

※話者スタイルの指定は `話者名.スタイル名>`（例: `ずんだもん.あまあま>`）で行います。
※`.font()` / `.tatie()` / `.size()` / `.color()` は現状パラメータの受け渡し・検証のみ対応しており、実際の見た目への反映（フォント適用や立ち絵表示など）は未実装です。

---

## 3. 評価優先順位 (Cascading Rules)

上から順に評価される中で、同パラメータが競合した場合は以下の優先度順で適用されます：

```
[1. 行直後の直接指定 (.speed(1.8).tatie("ドヤ顔"))]  <-- 最優先
       ↓
[2. 該当行までに現れたキャラ個別 preset (preset.'ずんだもん'.speed(1.25))]
       ↓
[3. 該当行までに現れた全体 preset (preset.all.speed(1.1))]
       ↓
[4. システムデフォルト値]
```

---

## 4. ショートカット ＆ 爆速スニペット一覧

```smd
// Shift + Enter      : カーソル行の1台詞のみ単行再生
// 右上 [再生] ボタン  : 現在行からの連続スクリプト自動再生 (エディタ切替時即全停止)
// preset + Enter     : preset. に一発展開 (all や "話者名" サジェストが全自動オープン)
// zunda + Enter      : 行頭で ずんだもん> に一発展開
// metan + Enter      : 行頭で 四国めたん> に一発展開
// se + Enter         : .se('/se/') に一発展開 (挿入直後に /se/ パス補完が全自動オープン)
// サジェスト時の ↑/↓  : 補完候補の選択 ＆ 試聴音声の何度でも反復再生
```

---

## 5. ユーザーカスタマイズ ＆ 設定のオーバーライド (Custom Configuration)

ScriptMarkDown は、ショートカットキーによる話者名挿入やキャラクターの定義（名前、カラー、対応スタイル名、voicevoxID）を、VS Code の標準設定 (`settings.json`) で直感的にカスタマイズ・オーバーライド（上書き）できます。

### オーバーライドの階層構造 (Priority)

```
[1. ワークスペース設定 (.vscode/settings.json)]  <-- 最優先 (該当プロジェクト限定)
       ↓
[2. ユーザー共通設定 (Global settings.json)]    <-- 全プロジェクト共通
       ↓
[3. 拡張機能内蔵プリセット (presets.json)]        <-- デフォルト初期値
```

### `settings.json` でのカスタマイズ例

ユーザー設定（グローバル）、またはプロジェクト内の `.vscode/settings.json` に記述することで、キーショートカットやキャラクター定義を自由自在に上書きできます。

```json
{
  // 1. ショートカットキー (Cmd+1〜9 / Ctrl+1〜9) で挿入される話者名をカスタマイズ
  "smd.shortcuts": {
    "1": "ずんだもん> ",
    "2": "四国めたん> ",
    "3": "オリジナルキャラ> "
  },

  // 2. キャラクターの表示色や定義のカスタマイズ・新規追加
  "smd.speakers": [
    {
      "id": "shikoku_metan",
      "color": "#00ffff"
    },
    {
      "id": "my_custom_speaker",
      "name": "オリジナルキャラ",
      "aliases": ["オリジナルキャラ", "オリキャラ"],
      "styles": ["ノーマル"],
      "styleIds": [0],
      "defaultStyle": "ノーマル",
      "color": "#ff007f"
    }
  ]
}
```
